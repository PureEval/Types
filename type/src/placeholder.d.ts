export const _: symbol
export const unreached: symbol
export const otherwise: symbol

// mainly for internal use
export type $_ = typeof _
export type $unreached = typeof unreached
export type $otherwise = typeof otherwise
